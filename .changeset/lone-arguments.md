---
'saykit': patch
'@saykit/react': patch
'@saykit/transform-js': patch
'@saykit/transform-jsx': patch
---

Format a lone `say.date`, `say.number`, `say.time` or `<Say.Date />` directly instead of extracting it, since it carries nothing a translator can change
